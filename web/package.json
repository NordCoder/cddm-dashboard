{
  "name": "cddm-dashboard-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc && node scripts/copy-assets.mjs",
    "test": "npm run clean && tsc && node --test tests/*.test.mjs",
    "dev": "npm run build && node scripts/dev-server.mjs"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "react-test-renderer": "18.3.1",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
