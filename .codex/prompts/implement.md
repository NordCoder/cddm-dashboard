Use `$cddm-implement` for Issue #{{ISSUE}}.

Work only in the current Change worktree/branch. Read `AGENTS.md`, the Active Milestone, the current Issue, the canonical Change Contract, and relevant code/tests.

Start only if the Change is implementation-ready and dependencies are satisfied. Implement the smallest maintainable in-scope solution, run practical V1/V2, inspect the final diff, commit and push the Candidate, and create or reuse the current draft PR. Mark it ready only after Candidate-ready verification.

Do not expand Scope or implement future Changes. Persist exactly one compact IMPLEMENT result to the PR.
