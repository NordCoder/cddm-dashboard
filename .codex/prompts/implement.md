Use `$cddm-implement` for Issue #{{ISSUE}}.

Work only in the current Change worktree. Read `AGENTS.md`, the Active Milestone, the current Issue context supplied below, the canonical Change Contract, and relevant code/tests.

Start only if the Change is implementation-ready and dependencies are satisfied. Implement the smallest maintainable in-scope solution, run focused local checks available without network access, and inspect the final diff. Do not stage, commit, push, use GitHub, or write delivery state; host-side orchestration runs authoritative full V2 and publishes the Candidate only after your `DONE` result.

Final response MUST be exactly the `$cddm-implement` result schema with no prose or code fence.