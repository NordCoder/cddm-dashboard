Use $cddm-implement to continue Change #{{ISSUE}} in this existing persistent Change session.

Canonical Change Contract: `{{CONTRACT}}`.

Preserve the existing repository understanding, Goal context, and private Tasks where still valid. Do not re-plan WHAT or HARD HOW.

The Web Lead instruction for this turn is:

{{LEAD_INSTRUCTION}}

If the instruction conflicts with the approved Change Contract or requires a HARD HOW change, return BLOCKED with concise evidence instead of redesigning the architecture.

If the previous Goal is complete and a bounded correction remains, update/create a Goal for this correction phase if goal tools are available. Goal state remains an execution aid, not delivery authority.

Return only the structured result required by the output schema.
