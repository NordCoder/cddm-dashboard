Use $cddm-implement to continue Change #{{ISSUE}} in this existing persistent Change session.

Canonical Change Contract: `{{CONTRACT}}`.

Stable Change Goal:

Implement approved Change #{{ISSUE}} to locally candidate-ready state while preserving every Requirement, Out of Scope boundary, and HARD HOW decision in `{{CONTRACT}}`.

Preserve the existing repository understanding, this same Goal, and private Tasks where still valid. Do not replace the Change Goal for a bounded correction. Translate the Web Lead instruction into one or more private Tasks under the same Goal.

The Web Lead instruction for this turn is:

{{LEAD_INSTRUCTION}}

If the instruction conflicts with the approved Change Contract or requires a HARD HOW change, return BLOCKED with concise evidence instead of redesigning the architecture.

Return only the structured result required by the output schema.
