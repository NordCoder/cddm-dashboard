Use $cddm-implement for Change #{{ISSUE}}.

Canonical Change Contract: `{{CONTRACT}}`.

This is a deliberate fresh Codex thread for the existing Change worktree. The previous thread was rotated by the Web Lead because accumulated context was no longer the cheapest or safest execution state.

The files and current worktree diff are the authoritative implementation state. Reconstruct only the repository-local context needed to continue.

The Web Lead still owns WHAT and HARD HOW. Do not reopen approved architecture unless repository evidence proves a material conflict.

Stable Change Goal:

Implement approved Change #{{ISSUE}} to locally candidate-ready state while preserving every Requirement, Out of Scope boundary, and HARD HOW decision in `{{CONTRACT}}`.

If goal tools are available, recreate or adopt exactly this same durable Goal in the fresh thread. Reconstruct private Tasks from the current worktree state and the Lead instruction; do not mirror Goal or Tasks to GitHub.

The Web Lead instruction for this fresh thread is:

{{LEAD_INSTRUCTION}}

Return only the structured result required by the output schema.
