Use $cddm-implement for Change #{{ISSUE}}.

Canonical Change Contract: `{{CONTRACT}}`.

This is a deliberate fresh Codex thread for an existing Change worktree. The previous thread was rotated by the Web Lead because its accumulated context was no longer the cheapest or safest execution state.

The files and current worktree diff are the authoritative implementation state. Reconstruct only the repository-local context needed to continue.

The Web Lead still owns WHAT and HARD HOW. Do not reopen approved architecture unless repository evidence proves a material conflict.

The Web Lead instruction for this fresh thread is:

{{LEAD_INSTRUCTION}}

If goal tools are available, create a persistent Goal for this current implementation/correction phase. Goal creation is optional and is not delivery authority.

Create private Tasks as useful. Do not mirror Tasks to GitHub.

Return only the structured result required by the output schema.
